name: adapt_log_device_app_info_input_adapter
description: Input adapter que captura informações do dispositivo e do app (modelo, SO, versão) e as inclui nos registros de log.
version: 1.0.0
repository: https://github.com/ranierjardim/dart_adapt_log

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  adapt_log:
    path: ../adapt_log/
  device_info_plus: ^10.0.0
  package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
