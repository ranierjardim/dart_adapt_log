name: adapt_log
description: Core do ecossistema dart_adapt_log — define os contratos de entrada e saída de log e orquestra o fluxo entre todos os adapters.
version: 1.0.0
repository: https://github.com/ranierjardim/dart_adapt_log

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
