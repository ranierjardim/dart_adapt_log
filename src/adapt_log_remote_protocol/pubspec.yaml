name: adapt_log_remote_protocol
description: Contrato de transporte entre o adapt_log e o adapt_log_server — formato JSON de lotes, sessões e eventos de stream.
version: 1.0.0
repository: https://github.com/ranierjardim/dart_adapt_log

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  adapt_log:
    path: ../adapt_log/

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
